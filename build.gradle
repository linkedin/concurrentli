apply plugin: 'idea'

//archivesBaseName = "concurrentli"

subprojects {
  plugins.withType(JavaPlugin) {
    group = 'com.concurrentli'
    version = '1.2.0'
    archivesBaseName = "concurrentli"
  }
}
